import { Fragment } from 'react';
import './Header.css'

const Header = () => {
  return (
    <Fragment>
      <header className="header">
        <h2>Drug App</h2>
      </header>
    </Fragment>
  );
};

export default Header;